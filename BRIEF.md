# BRIEF — TRAP HOUSE: сайт с билетами, QR-проходками и автообновлением из TG

Рабочий документ для встречи с организатором.
Демо: **https://traphouse-demo.vercel.app** — показывать с телефона.
Прод собран с ветки `claude/nightclub-events-website-mwxvoe`; после мержа в
`main` деплой обновляется с `main`.

Концепция бренда (по kirdydirty.xyz и каналу @trap_house56): дневные
БЕЗАЛКОГОЛЬНЫЕ вечеринки 14+, двери 16:00 / старт 17:00 / финиш 22:00,
«домой до темноты». Оренбург + Магнитогорск, выезды по заявкам городов.

---

## 1. Что уже есть в демо (показать вживую)

1. **Афиша с реальными фото** и волнами цен 300/400/500 ₽; счётчик «идут N».
2. **Покупка за минуту**: имена гостей → телефон → согласие → именные билеты.
3. **QR-проходка**: билет с QR, сканируется обычной камерой → светофор для
   админа (зелёный «Пропустить», красный «Уже использован в 19:41, впустил
   Артём» / «Подделка»). Скриншот чужого билета не проходит.
4. **Офлайн-выживаемость**: подпись без БД, офлайн-список на телефоне,
   впуск «под запись» с досинхронизацией, печатный список.
5. **Панель организатора**: продано/выручка/вошло/осталось, волны, кривая
   входа, лента сканов.
6. **Фейсконтроль-интерактив** («Сколько тебе лет?» → «Ты в списке» /
   «Рано» / «Ну ладно» + «скопировать и похвастаться») — виральная механика
   в духе их сайта, свои тексты.
7. **«Привезите к нам»**: чипы городов (Уфа, Самара, Челябинск, Казань,
   Саратов…) + заявка города → уведомление организатору в TG.
8. **Секция «Родителям»** и FAQ под 14+ (0% алкоголя, браслеты, регламент).
9. **Автообновление сайта из TG-канала** (главная новая фича — раздел 3).

Проверено: 75 юнит/SQL-тестов (Postgres) + 20-шаговый браузерный e2e — зелёные.

## 2. Чем это сильнее kirdydirty.xyz (факты из их сайта)

| У них (kirdydirty.xyz) | У нас |
|---|---|
| «Даты пока нет» — афиши не существует | Живая афиша из БД, обновляется из TG-канала |
| Билет = «пишешь @kosenko_alenaa, попадаешь в список ФИО» | Покупка на сайте за минуту, именной QR |
| Оплата на кассе, очередь на дверях | Оплачено заранее, вход по QR за секунды |
| Скидка 300 ₽ за репост — скрин проверяют вручную на входе | Ранняя волна 300 ₽ делает то же честно и без проверки скринов |
| Повторный вход — браслет «на глаз» | Одноразовый QR + журнал: кто, когда, кто впустил |
| Выручку и посещаемость никто не видит | Панель + все данные в БД под TG-бота владельца |
| Видео «скоро», статичный сайт | Афиша обновляется сама при посте в канале |

Что у них хорошо и уже перенято (в своей редакции): тексты-манифест,
интерактив-фейсконтроль, секция родителям, лайм-теги на фото, «привезите к нам».

## 3. Автообновление из канала @trap_house56 (как включить)

Пост в канале → анализ (Claude) → черновик события в БД → владельцу в TG
приходит превью с кнопками **«Опубликовать / Пропустить»** → одна кнопка,
и событие на сайте (redeploy не нужен). `AUTO_PUBLISH=1` — полный автопилот
без подтверждения. Афиша-картинка поста подтягивается автоматически.
Без ANTHROPIC_API_KEY посты просто пересылаются владельцу с пометкой
«обнови вручную» — ничего не ломается.

Настройка (5 минут, после env из раздела 7):
1. Добавить бота (того же, чей `TELEGRAM_BOT_TOKEN`) **администратором
   канала** @trap_house56 (право «читать сообщения» достаточно).
2. Зарегистрировать вебхук (одна команда, подставить свои значения):
   `curl "https://api.telegram.org/bot<ТОКЕН>/setWebhook?url=https://<домен>/api/tg-webhook&secret_token=<TG_WEBHOOK_SECRET>"`
3. Готово: каждый новый пост канала попадает в конвейер мгновенно.

**Провайдер анализа — Polza AI** (российский OpenAI-совместимый агрегатор,
оплата в рублях, ключ на polza.ai): env `POLZA_API_KEY` + опционально
`POLZA_MODEL` (по умолчанию `openai/gpt-4o-mini`) и `POLZA_BASE_URL`
(по умолчанию `https://api.polza.ai/api/v1`). Смена модели = смена одной env.
`ANTHROPIC_API_KEY` — необязательный фолбэк (используется, только если
Polza-ключа нет). Без обоих ключей посты пересылаются владельцу вручную.

Экономика при 1–2 постах в день (~62 вызова/мес, ~1100 токенов промпт +
~300 ответ): на gpt-4o-mini — копейки; даже на полноразмерном gpt-4o
(7,5/22,5 ₽ за 1M токенов на Polza) — порядка 1 ₽/мес. Предохранители:
пост обрезается до 3000 символов, max_tokens 700, temperature 0, ровно один
вызов на пост (повторные апдейты отсекаются по update_id). Бюджет 500 ₽/мес
недостижим даже теоретически.

## 4. Вопросы владельцу (★ — блокируют боевой запуск)

1. ★ Даты/площадки ближайших тус (сид сейчас — правдоподобные заглушки
   BACK TO SCHOOL 05.09 и MAGNITKA MOVE 19.09).
2. ★ Как сейчас принимают деньги; целевой провайдер онлайн-оплаты
   (ЮKassa / СБП). Реквизиты ИП для политики ПД и кассы 54-ФЗ.
3. ★ @kosenko_alenaa — чей аккаунт (менеджер?), куда слать уведомления
   о продажах (chat_id) и кто жмёт кнопки публикации.
4. ★ Домен: покупаем короткий (компактнее QR)? Ссылка VK (сейчас заглушка).
5. Судьба «парного танцпола» (двое по цене одного) и репост-скидки —
   переносим в тарифы или закрываем ранней волной?
6. Точные даты/названия для подписей фотоленты (сейчас по мотивам их сайта).
7. Автопилот публикации: сразу AUTO_PUBLISH=1 или сперва с подтверждением?
8. Логотип/шрифтовый знак — есть ли исходники.

## 5. Юридические обязательства

Сделано: согласие 152-ФЗ в обеих формах, privacy.html (noindex, блок под
реквизиты), без счётчиков, секреты в env, честные дисклеймеры 14+.

Перед боем: реквизиты в политику; локализация ПД (ст. 18 ч. 5 152-ФЗ —
Vercel это США, боевой контур на RU-хостинг отдельным этапом); уведомление
РКН — обязанность владельца (предупредить письменно); онлайн-оплата =
эквайринг + касса 54-ФЗ; допуск/контроль несовершеннолетних — зона
ответственности организатора и площадки, сайт даёт маркировку и учёт.

## 6. Смета (ориентиры, финал после ★)

| Этап | Состав | Ориентир |
|---|---|---|
| 1. Сайт + билеты + QR-вход (готово в демо) | всё из раздела 1, перенос на боевой домен, RU-контур ПД | 60–90 тыс, первому клиенту скидка |
| 2. Автообновление из канала (готово в демо) | конвейер TG→сайт, подтверждение кнопкой, автопилот | 25–40 тыс |
| 3. Онлайн-оплата | ЮKassa/СБП, вебхуки, TTL-брони, чеки | 25–45 тыс |
| 4. TG-бот владельца | продажи в боте, аналитика, рассылки | 35–60 тыс |
| Обслуживание | хостинг, домен, присмотр, N часов правок, LLM-анализ постов | 6–10 тыс/мес |

## 7. Технический паспорт (запуск)

Стек: статика + ES-модули (без сборки) · Vercel serverless · Neon Postgres ·
Claude Haiku для анализа постов.

Env-переменные (Vercel → Settings → Environment Variables):
`DATABASE_URL` (Neon из Marketplace) · `TICKET_SECRET` (32+ случайных символов) ·
`ADMIN_KEY` (ключ админов входа) · `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID`
(уведомления и кнопки) · `TG_WEBHOOK_SECRET` (для вебхука канала) ·
`POLZA_API_KEY` (анализ постов через Polza AI; `POLZA_MODEL` и
`POLZA_BASE_URL` — опциональные переопределения) · опционально
`ANTHROPIC_API_KEY` (фолбэк анализа), `AUTO_PUBLISH=1`, `BOT_API_TOKEN`,
`TICKET_SECRET_OLD`.

**Подключение БД (Neon, 3 шага):**
1. Vercel → проект → вкладка **Storage** → **Create Database → Neon** (Marketplace,
   Free plan) → **Connect to project** — `DATABASE_URL` добавится в env сам.
2. **Redeploy** (Deployments → ⋯ у последнего → Redeploy), чтобы функции
   увидели новые переменные.
3. Открыть `/admin.html` → ввести `ADMIN_KEY` → кнопка **«Инициализировать БД»**
   (галочка «посеять демо-продажи» — для показа; снять — для боя).
   Эквивалент curl: `curl -X POST https://<домен>/api/seed -H "X-Admin-Key: <ключ>"
   -H "Content-Type: application/json" -d '{"demoSold":true}'`

**Боевой самотест** — кнопка в `/admin.html`: прогоняет на проде весь цикл
(покупка → билет → верификация → чек-ин → повтор отклонён → статистика →
уборка тестовых данных с возвратом квот) и рисует чек-лист. Ключ при этом
не покидает устройство. Повторять можно сколько угодно.

Афиша правится: постом в канале (конвейер) или в `assets/data/events.js` + seed.

Тесты: `npm test`. Локально без Neon: `DEV_PGLITE=1` (настоящий Postgres в памяти).
Деградации: без БД — сид + TG-фолбэк заказа; без LLM — пересылка постов;
без TG — сайт работает, уведомлений нет. Ошибок гость не видит нигде.

**Секреты:** только в env. Ключ, засветившийся в переписке, — перевыпустить.
