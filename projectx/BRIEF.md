# BRIEF — PROJECT X: сайт с проходками, QR-входом и афишей из админки

Рабочий документ для встречи с организатором.
Демо: **https://proxject.ru** — показывать с телефона.
Код живёт в папке `projectx/` общего репозитория (Vercel-проект с Root
Directory = `projectx`); прод обновляется с ветки `main`. Отдельный продукт
со своей БД и своими env — с TRAP HOUSE и ECHO ничего не пересекается.

Концепция бренда (снята с реальных афиш @project.x.prty): **ночные вечеринки
строго 18+** в Оренбурге, **FC/DC** (фейсконтроль и дресс-код), **двери 22:00,
старт 23:00**, финиш под утро. Фирменная фишка — **SECRET PLACE**: площадка
объявляется позже, адрес приходит в проходке. Ядро аудитории — студенты
(посвящение первокурсников, ночь студента, Хэллоуин, американ-пати с red cups
и free bar). У бренда есть второй аккаунт (@project.x.tolyatti), но по
решению заказчика сайт пока только про Оренбург — Тольятти нигде не упоминается.

---

## 0. Что мы уже знаем о вас (и почему сайт собран именно так)

Разобрали ваши афиши: логотип PRO✕JECT (белые буквы, алый крест), чёрно-красная
гамма, гранж-фактура, FC/DC 18+, двери 22:00 / старт 23:00, SECRET PLACE,
резиденты (DJ SHENDI, DJ JOKER, DJ DYSSHA, DJ DIZAYNER, ASIO, ARTURQUE,
VANULA и другие), площадки CLUB CTUDИЯ (Алтайская 5/1), «РЕЖИССЁР»
(Волгоградская 46/3), ГИГАНТ ХОЛЛ. Сайт собран в этой же гамме и на этих же
фактах — не «шаблон под клуб», а ваш бренд. Афиши с постов стоят в афише и
фотоленте; заменим на оригиналы, как только пришлёте.

Отдельно видно, что аудитория живая: под постами 4 248, 1 175, 659, 615, 398
комментариев. Эти люди сейчас упираются в стену: чтобы попасть на вечеринку,
нужно быть подписанным, увидеть пост и написать в директ. Сайт снимает это
ограничение и превращает интерес в оплаченную проходку.

## 1. Главный аргумент: Instagram в РФ заблокирован

Аккаунт бренда живёт за VPN. Практические следствия, которые видно по нашей
же разведке (15+ поисковых запросов, все зеркала — Picuki, Imginn и прочие):
**из открытого интернета о PROJECT X нельзя узнать вообще ничего** — ни имени,
ни афиши, ни цен. Поисковику Instagram отдаёт только страницу входа.

Сайт закрывает эту дыру: он индексируется, открывается без VPN у кого угодно
(включая родителей, партнёров и площадки), и принимает деньги. Instagram
остаётся витриной для своих, сайт — точкой входа для всех остальных.

Отдельно: по запросу «project x вечеринка» вся выдача принадлежит чужим
брендам — московскому PROJECT X (дневные пати для школьников, TimePad) и
франшизе «PROJECT X | Проект икс» (18+ с безлимитным алкоголем, 8+ городов).
Свой сайт с привязкой к городу («PROJECT X Оренбург» в title и OG) —
единственный способ не потеряться среди них. Это же довод за короткий
собственный домен: связка «prty» уже занята чужой франшизой.

## 2. Что уже есть в демо (показать вживую)

0. **Кинематографичная главная**: полноэкранный знак PROJECT X, который живёт
   под курсором и наклоняется на прокрутке; сцена «три правила ночи» (18+ /
   FC/DC / SECRET PLACE) раскрывается по мере скролла; лента прошедших афиш
   листается как стопка постеров; манифест в три фразы. Главная короткая и
   ведёт к одному действию: взять проходку. Всё движение уважает системную
   настройку «меньше движения».
0а. **У каждого пункта меню — своя страница**: /afisha (ближайшие ночи +
   стена постеров всех прошедших), /night (три правила + таймлайн ночи от
   дверей до утра + резиденты), /fc (фейсконтроль-интерактив, дресс-код
   «проходит / разворачиваем», чеклист «с собой», как проходит вход),
   /rules, /faq, /contacts. Шапка, меню и футер собираются из одного шаблона
   (`scripts/sync-chrome.mjs`) — правка в одном месте.
1. **Афиша ночных событий** с волнами цен. Ночь 26.09 — одна цена 1000 ₽
   и скрытый гостевой список (продаёт только касса).
2. **Проходка за минуту без платёжного шлюза**: имена гостей → телефон →
   согласие → **бронь** с кодом (PX-7F3K) и реквизитами СБП → гость нажимает
   «Я перевёл» → владелец подтверждает в панели или кнопкой в Telegram →
   проходки активны, бот присылает их гостю. **Тот же путь целиком внутри
   Telegram-бота** (/buy: количество → номер кнопкой → имена → бронь) — для
   тех, кто живёт в мессенджере; источник таких броней в панели — `tgbot`. Неоплаченная бронь сгорает
   через 3 часа (в день ночи — через час), места возвращаются. На входе
   неоплаченную бронь принимают наличными одной кнопкой сканера.
3. **QR-вход**: сканируется обычной камерой телефона → светофор для админа
   (зелёный «Пропустить», красный «Уже использован в 01:41, впустил Артём» /
   «Подделка»). Скриншот чужой проходки не проходит.
4. **Возраст**: все события 18+, несовершеннолетним проходка не продаётся —
   попытка отсекается на сервере, а не «на честном слове».
5. **Офлайн-выживаемость**: подпись без БД, офлайн-список на телефоне, впуск
   «под запись» с досинхронизацией, печатный список.
6. **Панель организатора**: оплачено/выручка/ждут оплаты/вошло/осталось,
   переводом vs на входе, **ожидающие брони с подтверждением и отменой**,
   продажи по дням и **по источникам** (метки ссылок, QR-постеры для
   промоутеров и общаг), кривая входа, лента сканов, касса на дверях,
   переоформление и аннулирование проходок, боевой самотест кнопкой.
6а. **Два ключа**: админский — всё; **ключ двери** — только сканер, чек-ин,
   приём оплаты на входе и офлайн-список. Хостес не видит кассу и не правит
   ночи; ушёл сотрудник — меняется один ключ.
7. **★ Афиша из админки** — ключевая фича именно для PROJECT X (раздел 3).
8. **Фейсконтроль-интерактив** («Сколько тебе лет?» → «Рано» / «Проходишь»)
   и «Привезите к нам» — заявки из других городов.
9. **Правила входа и FAQ** под ночной формат: паспорт, дресс-код, браслет,
   передача проходки, возврат — на отдельных страницах /rules и /faq
   (аккордеон с плавным раскрытием).

Проверено: 138 юнит/SQL-тестов (настоящий Postgres, включая покупку в боте:
мастер, смена цены при распроданной волне, потолок мест, настройка бота) +
браузерный e2e: 87 шагов общей регрессии, 26 шагов сценария брони (бронь →
«я перевёл» → подтверждение в панели → проходка ожила → дверь по ключу двери →
неоплаченная бронь принята наличными → отмена → границы ролей) и 19 шагов
жестов и доступности — зелёное.

## 3. ★ Афиша из админки (вместо автообновления из канала)

У TRAP HOUSE афиша обновляется постами Telegram-канала. У PROJECT X канала
нет, поэтому источником стала сама панель: раздел **«События»** —
создать вечеринку, задать дату, двери/финиш, площадку, описание и волны цен,
нажать «Сохранить» — сайт обновился. Никакого разработчика и редеплоя.

Что предусмотрено, чтобы владелец не сломал продажи:
- **Черновик** (статус «черновик») виден только в панели — можно готовить
  вечеринку заранее и опубликовать одной сменой статуса;
- **квота не опускается ниже проданного** — сервер поднимает её обратно и
  говорит об этом («уже продано 12 — квота поднята до 12»);
- **волну с продажами нельзя удалить** — на неё ссылаются заказы гостей;
- **id события не меняется** при правках — уже выданные QR продолжают работать;
- **ночь через полночь** (23:00 → 06:00) считается правильно: финиш
  автоматически уезжает на следующий день, время всегда в поясе площадки.

Конвейер анализа постов из кода не убран: если у бренда появится Telegram-канал,
он включается парой env-переменных (`TELEGRAM_BOT_TOKEN`, `TG_WEBHOOK_SECRET`,
`POLZA_API_KEY`) и кнопкой «Настроить бота» в панели — сейчас он просто спит.

## 4. Вопросы владельцу (★ — блокируют боевой запуск)

Решено 16.09.2026: ночь 26.09 в «Режиссёре» (адрес открыт), цена 1000 ₽,
оплата переводом на карту Максима с подтверждением в панели, вместимость
до 220 (продаём 200 + гостевой список 10), именные проходки, возврат до
24.09, на входе две хостес и два охранника, интернет есть, бот — будет.

Решено 17.09.2026 (по афише и посту владельца): название «PROJECT X —
БЕСПЛАТНЫЙ БАР», площадка «Арт-локация «Режиссёр»» (Волгоградская 46/3),
до 04:00; цены по посту — первые 50 проходок по 1000 ₽, дальше 1500 ₽ (две
публичные волны + скрытый гостевой список; 18.09 владелец поставил в панели
вторую волну 1200 ₽ — боевые цены живут в базе, все тексты сайта и бота
собираются из волн через `ladderText`, править цифры в HTML не нужно); FREE BAR на сайте включён
(`SITE.showFreeBar`) без марок и названий напитков; афиша — постер ночи
(`assets/photos/poster-260926.jpg`) и OG-картинка (`og-260926.jpg`); видео
с прошлых ночей — немой луп в hero главной (`assets/video/party-loop.mp4`,
12 с, 1 МБ) и афтемуви со звуком по клику (`aftermovie.mp4`, 34 с, 4,5 МБ)
на главной и /night. Электрические кальяны из поста на сайт не вынесены
(реклама устройств для потребления табака и никотина запрещена) — при
желании добавляются в описание ночи из панели. Без базы симуляция продаж
показывает только первую волну и не выше 40 из 50.

Добавлено 18.09.2026: домен **proxject.ru** (основной без www; www —
редирект), `SITE.siteUrl`; страница **/promo** — личная ссылка промоутера с
меткой `?src=`, QR и тексты для сторис (метка запоминается на сессию, поэтому
ссылка может вести и на главную; продажи видны в панели по источникам);
**Яндекс.Метрика** по `SITE.metrikaId` с целями `booking_open`,
`booking_done` (сумма, количество), `booking_fallback`; `robots.txt` и
`sitemap.xml`; canonical на страницах.

1. **Название ночи и афиша** — на сайте с 17.09 (сид и `poster-260926.jpg`).
   Имена диджеев — в панель («События»), как только известны.
2. ★ **Реквизиты СБП** (номер, банк, как показывается получатель) —
   `assets/data/config.js → transfer`. Они публичные, гость видит их на экране брони.
3. **Материалы в оригинале**: логотип PRO✕JECT векторно. Видео с прошлых
   ночей получено 17.09 (`assets/video/`); фото с ночей — тоже пригодятся.
4. ★ **Neon + переменные** (раздел 7): без базы бронь на проде не работает.
5. ★ **Бот**: создать в BotFather, токен — в `TELEGRAM_BOT_TOKEN`, имя бота
   (без @) — в `TELEGRAM_BOT_USERNAME`, chat_id владельца — в `TELEGRAM_CHAT_ID`,
   случайная строка — в `TG_WEBHOOK_SECRET`, Redeploy, затем в панели
   (блок «Сервис») кнопка **«Настроить бота»** (раздел 7). Гость покупает
   прямо в боте: /buy → сколько → телефон → имена → бронь → реквизиты.
   Без бота всё работает через сайт и панель.
6. **Домен**: покупаем короткий (компактнее QR и не путается с чужими
   PROJECT X)? Варианты обсудим с учётом раздела 1.
7. Связь с TRAP HOUSE: упоминаем на сайте («от команды TRAP HOUSE») или
   держим бренды раздельно? Имя TRAP HOUSE в нише занято другими
   организаторами, поэтому вопрос не праздный.
8. Возрастной формат подтверждаем: строго 18+, без 16+ вечеринок?
9. Нужен ли раздел «Столы / компании» — бронь стола с депозитом.
10. Когда дойдёт до Тольятти: платформа уже умеет несколько городов — это
    одна строка в конфиге плюс фильтр на афише, который сейчас спрятан.

## 5. Юридические обязательства

Сделано: полная политика конфиденциальности `/privacy` по 152-ФЗ (оператор
и связь, таблица «когда / какие данные / зачем / основание» по всем формам,
боту, входу и техданным, гости в брони, где хранится и кто видит, сроки,
cookie и память браузера, права и порядок запросов, защита, 18+, изменения,
текст согласия `#consent`, на который ведут чекбоксы форм и кнопка бота);
согласие 152-ФЗ в обеих формах и в боте; реквизиты оператора живут в
`SITE.legal` (`assets/data/config.js`) и подставляются в политику и в футер
всех страниц — пустые поля не выводятся; сноска про Meta везде, где есть
ссылка на Instagram; без счётчиков и трекеров (Метрика только по
`SITE.metrikaId`, политика показывает её статус); секреты только в env;
честная маркировка 18+.

Перед боем: заполнить `SITE.legal` (полное ФИО как в ЕГРИП, ИНН, ОГРНИП,
адрес, e-mail для запросов); подать уведомление в Роскомнадзор об обработке
ПД (Госуслуги, бесплатно) — обязанность владельца; локализация ПД
(ст. 18 ч. 5 152-ФЗ — Vercel и Neon это США, боевой контур на RU-хостинг
отдельным этапом; политика называет провайдеров честно); онлайн-оплата =
эквайринг + касса 54-ФЗ.
Продажа алкоголя и контроль возраста на площадке — зона ответственности
организатора и площадки; сайт даёт маркировку 18+, возрастную категорию в
проходке и журнал входа.

## 6. Смета (ориентиры, финал после ★)

| Этап | Состав | Ориентир |
|---|---|---|
| 1. Сайт + проходки + QR-вход + афиша из админки (готово в демо) | всё из раздела 2 и 3, боевой домен, RU-контур ПД | 45–65 тыс |
| 2. Онлайн-оплата | ЮKassa/СБП, вебхуки, TTL-брони, чеки | 25–45 тыс |
| 3. TG-бот владельца | продажи и аналитика в боте, рассылки | 35–60 тыс |
| 4. Автообновление из канала (код готов, спит) | включение конвейера, если появится Telegram-канал | 10–20 тыс |
| Обслуживание | хостинг, домен, присмотр, N часов правок | 6–10 тыс/мес |

## 7. Технический паспорт (запуск)

Стек: статика + ES-модули (без сборки) · Vercel serverless · Neon Postgres.
Vercel-проект: `projectx-party`, Root Directory = `projectx`, продакшен с `main`.

Env-переменные (Vercel → projectx-party → Settings → Environment Variables) —
**свои, ничего от TRAP HOUSE и ECHO не переиспользовать**:
`DATABASE_URL` (СВОЯ база Neon) · `TICKET_SECRET` (32+ случайных символов,
свой) · `ADMIN_KEY` (панель и всё) · **`DOOR_KEY`** (ключ двери: только
сканер, чек-ин, касса на входе) · `PAYMENT_MODE` (`transfer` по умолчанию —
бронь с переводом; `demo` — только для демо-стенда) · `SITE_ORIGIN`
(`https://proxject.ru` — для ссылок в сообщениях бота) ·
для бота: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME` (без @),
`TELEGRAM_CHAT_ID` (chat_id владельца — туда идут брони и кнопки
«Подтвердить / Не пришло»), `TG_WEBHOOK_SECRET` (случайная строка для
setWebhook) · опционально `POLZA_API_KEY` (спящий конвейер из канала),
`AUTO_PUBLISH=1`, `BOT_API_TOKEN`, `TICKET_SECRET_OLD`.

**Откуда взять переменные бота.** `TELEGRAM_BOT_TOKEN` выдаёт BotFather при
создании бота (/newbot), там же видно имя для `TELEGRAM_BOT_USERNAME`.
`TELEGRAM_CHAT_ID` — числовой id личного чата владельца: напиши боту
@userinfobot (или @getmyid_bot), он ответит твоим `Id`; этот же аккаунт
должен первым написать нашему боту /start, иначе Telegram не даст боту
писать тебе. `TG_WEBHOOK_SECRET` — любая случайная строка 30+ символов
(латиница и цифры), её знает только Vercel. `SITE_ORIGIN` =
`https://proxject.ru`.

**Бот (после того как токен и секрет лежат в Vercel и сделан Redeploy):**
панель → блок «Сервис» → **«Настроить бота»**. Сервер сам регистрирует
вебхук `https://proxject.ru/api/tg-webhook` с секретом, меню команд
(/buy, /tickets, /cancel) и описание бота; результат виден под кнопкой
(в том числе последняя ошибка Telegram и несовпадение имени бота с
`TELEGRAM_BOT_USERNAME`). Кнопку можно жать сколько угодно. Адрес вебхука
сервер проверяет сам и следует редиректу (proxject.ru → www.proxject.ru):
Telegram по редиректам не ходит, на голом домене бот бы молчал. Таблицы,
добавленные релизом после «Инициализировать БД», функция бота доводит сама
при первом сообщении — нажимать инициализацию заново не нужно.
Проверка: написать боту `/start` — он покажет ближайшую ночь и кнопку
«Забронировать»; пройти бронь до реквизитов, затем в панели отменить
её (или подтвердить кнопкой в Telegram и проверить, что проходки пришли).

**Что умеет бот для гостя.** /buy (или кнопка в приветствии): количество
(кнопки 1–4 или число до 10) → телефон (кнопка «Отправить мой номер»)
→ имена гостей → сводка с подтверждением 18+ и правил → бронь тем же
ядром, что и сайт (`api/_lib/order-core.js`: цена только из базы, квота
списывается атомарно). Если волна кончилась, пока гость заполнял, бот
предлагает новую цену на всю компанию; если мест меньше, чем просят, —
просит меньшее число. После брони — реквизиты, код и кнопка «Я перевёл»;
после подтверждения владельцем — проходки в этот же чат. Источник таких
броней в панели — `tgbot`. Состояние мастера живёт в `tg_sessions`
(2 часа), любая команда его сбрасывает.

**Подключение БД (Neon, 3 шага):**
1. Vercel → проект → **Storage → Create Database → Neon** (Free) →
   **Connect to project** — `DATABASE_URL` добавится сам.
2. **Redeploy** (Deployments → ⋯ → Redeploy), чтобы функции увидели переменные.
3. `/admin.html` → ввести `ADMIN_KEY` → **«Инициализировать БД»**
   (галочка «посеять демо-продажи» на боевой базе должна быть СНЯТА).
   Сид создаёт ночь 26.09 с одной ценой 1000 ₽ и гостевым списком; название,
   афишу и диджеев затем поправить в разделе «События».
   Эквивалент curl: `curl -X POST https://<домен>/api/seed -H "X-Admin-Key: <ключ>"
   -H "Content-Type: application/json" -d '{"demoSold":true}'`

**Боевой самотест** — кнопка в `/admin.html`: прогоняет на проде весь цикл
(бронь → до оплаты не проходит → подтверждение → проходка → верификация →
чек-ин → повтор отклонён → статистика → уборка тестовых данных) и рисует
чек-лист. Ключ не покидает устройство, повторять можно сколько угодно.

Афиша правится в разделе «События» админки (раздел 3) или в
`assets/data/events.js` + повторный сид.

Тесты: `npm test` внутри `projectx/`. Локально без Neon: `DEV_PGLITE=1`
(настоящий Postgres в памяти). Деградации: без БД — сид + фолбэк оформления
в директ; без TG — сайт работает, уведомлений нет. Ошибок гость не видит нигде.

Ограничение хостинга: сейчас 12 serverless-функций — ровно потолок тарифа
Vercel Hobby. Следующий эндпоинт (реальная оплата, этап 2) потребует Pro
или объединения функций.

**Секреты:** только в env. Ключ, засветившийся в переписке, — перевыпустить.


## 7а. Ночь 26.09: порядок для команды

**До ночи.** Neon подключён, сид сделан, в «Событиях» стоит название и афиша,
в `config.js` заполнены реквизиты СБП, `DOOR_KEY` выдан хостес (не админский),
на телефонах двери открыт `/scan.html` → «Я сотрудник» → ключ двери и имя.
В панели скачан офлайн-список на каждый телефон двери (на случай сети).

**Продажи.** Бронь приходит уведомлением в Telegram владельца (если бот
подключён) и в панель, раздел «Ожидают подтверждения». Пришёл перевод с кодом
— «Подтвердить» (в панели или кнопкой в Telegram). Перевод без кода — найти по
сумме и телефону в том же списке. Нет перевода — бронь сгорит сама.

**На входе.** Сканируют QR обычной камерой. Зелёный — «Впустить», выдать
браслет. Янтарный «Не оплачено» — взять наличные, «Принять и впустить».
Красный — не пускать (использован / подделка / бронь сгорела). Гость без
брони — «Оформить на входе» в панели с телефона админа (или хостес — через
сканер по QR-постеру ночи). Ушёл покурить — обратно по браслету.

**После.** Панель: оплачено / на входе / вошло / источники — это и есть отчёт
по договору. Переоформление и аннулирование — в списке гостей.

## 8. Критика сайта и что делать дальше

Три независимых ревью (воронка и конверсия, движение, продукт) — 45 находок.
Всё быстрое и блокирующее закрыто в коде (см. историю коммитов). Ниже — то,
что требует решения владельца или отдельного этапа.

### 8.1. Главные пробелы воронки

1. **Проходка живёт только внутри in-app браузера Instagram.** После оплаты
   ссылки на QR лежат на странице и в памяти этого браузера; наружу ничего не
   уходит. Закрыл сторис — потерял проходку и будущий адрес. Нужен канал
   доставки: Telegram-бот по deep-link («Получить в Telegram» одним тапом) и
   резервно SMS. Это фундамент для всего остального (адрес, напоминания,
   лист ожидания).
2. ~~**SECRET PLACE как продукт.**~~ **Сделано** — по вашей модели (8.3):
   купил проходку — адрес сразу в ней; всем остальным адрес открывается
   за сутки до ночи (на сайте и в Instagram). Раньше поле адреса утекало
   в публичную афишу сразу, теперь это невозможно.
3. ~~**Пустое состояние между ночами.**~~ **Сделано**: когда ближайшей ночи
   нет, hero и афиша показывают тизер даты и форму «напишем первым» —
   заявка уходит вам в Telegram. Тизер правится одной строкой в
   `assets/data/config.js` (`nextTeaser`). Лист ожидания при sold out —
   остаётся в планах.
4. **Социальное доказательство — симуляция.** «Уже идут N» считается до 40
   реальных продаж. Для демо это оживляет страницу, для боя — риск репутации.
   Решение владельца: оставить до реальных продаж или показывать только правду.

### 8.2. Идеи под именно этот бренд (по отдаче на трудоёмкость)

| Идея | Эффект | Труд. |
|---|---|---|
| Волны по времени + таймер «ранняя цена до …» | продажи | S |
| Проходки без имён: платит один, друзья вписывают себя по ссылке компании | конверсия групповых покупок, ноль «переоформи на Машу» | S–M |
| Метки источников и QR-постеры для общаг (`?src=dorm-ogu-3`) | понятно, что продаёт | S |
| Промокоды в чекауте (DVIZH, коды пабликов и промоутеров) | прямой рычаг + атрибуция | M |
| SECRET PLACE: раскрытие адреса по времени и кнопке | ядро бренда | M |
| Telegram-бот гостя: проходка в чат, адрес, напоминание, «мои проходки» | база гостей вне VPN | M |
| «Вернись»: пресейл для прошедших вход + персональный код | возвраты | S поверх бота |
| Лист ожидания при sold out | добор мест, аргумент за площадку больше | S–M |
| Битва вузов: чипы «твой вуз» в чекауте → живой рейтинг ОГУ/ОрГМУ/ОГАУ | виральность в вузовских чатах | S |
| Ключи по ролям: дверь ≠ владелец | гигиена перед боем | S |
| Переоформление, отзыв, возврат из админки | операционка | S / M с деньгами |
| Перки на проходке: FREE BAR, red cup, fast lane; выдача на сканере | средний чек | M |
| «Приведи друга»: реф-ссылка на проходке, награда перком | виральность | M |
| Пост-ивент: фото, афтер-муви (VK Video), оценка ночи по чек-ину | возвраты | M |
| «Пульс ночи» на проходке: сколько внутри, очередь на дверях | размазать пик | S–M |
| Столы и компании с депозитом | высокий чек | L, нужен шлюз |
| Apple Wallet-пасс | имидж | M, нужен Apple Developer |

### 8.3. SECRET PLACE: как это работает сейчас (реализовано)

Ваша модель: **купившим адрес виден сразу, городу — за сутки до ночи.**
Проходка даёт не только вход, но и знание раньше остальных — это и есть
довод взять её заранее.

- **Вписывайте адрес в панели сразу при создании ночи.** В публичную афишу
  он не попадёт: `/api/events` вырезает его, пока до старта больше суток.
- **Купившему** адрес приходит вместе с проходкой — отдельным блоком с
  кнопками «Яндекс Карты» и «2ГИС». Пока адрес не заполнен, там честное
  «Адрес появится здесь».
- **За 24 часа до старта** адрес автоматически появляется в афише и на
  странице ночи — для всех. Ничего нажимать не нужно, порог считается от
  времени начала (`assets/secret-place.js`, 8 тестов).
- Прошедшие ночи адрес не прячут — это уже история.

Что можно добавить позже: подсказка-район до раскрытия, пароль ночи для
дверей, заметка «вход со двора», пуш в Telegram в момент раскрытия.

### 8.4. Что нужно от владельца, чтобы идти дальше

Neon Postgres (без него демо-покупка на проде отвечает «оформим вручную»),
реальные фото и видео с вечеринок, логотип в оригинале, время раскрытия
адреса, промокоды, канал связи помимо Instagram (Telegram/MAX/телефон).
